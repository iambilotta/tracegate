"""A *invariant* file routes to the INV category by name."""


def test_invariant_holds():
    """
    @spec.given the invariant naming convention
    @spec.when  the adapter categorizes this test
    @spec.then  it lands in INV
    """
    assert True
