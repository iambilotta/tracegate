package it.housetreespa.gest.sample.domain;

/** Enum of the sample kinds (golden fixture for the enum branch of the domain-model diagram). */
public enum SampleKind {
    APPOINTMENT,
    CALL,
    VISIT
}
