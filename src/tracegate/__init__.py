"""tracegate — the verifiable layer that keeps requirements and code in lockstep.

Phase 0 harvest: the working housetree doc-generators, extracted and parameterized
so they run standalone against any target repo. See PLAN.md for the roadmap; the
core/adapter refactor is Phase 1.
"""
__version__ = "0.0.0"
